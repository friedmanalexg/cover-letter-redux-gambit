import React from 'react'

const LetterViewer = () => {
    const { user } = useSelector(state => state.user)


  return (
    <div>LetterViewer</div>
    // render the prose block that joins selected letter at position 1
    // render the prose block that joins selected letter at position 2
    // render the prose block that joins selected letter at position 3
  )
}

export default LetterViewer